Work Distributor System (Dockerized)


 How to Run 

Prerequisites:

- Docker installed on your system


Steps to Run:

1. Open terminal in the project root folder (where this README is located)

2. Build and run the app using Docker Compose:

docker-compose up --build



Visit http://localhost:8081 in browser.